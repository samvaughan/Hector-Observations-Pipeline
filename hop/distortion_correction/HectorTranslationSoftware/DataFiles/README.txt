Results of analyzing offsets Madusha proceduced from data of the 28th of Feb.

Results are now good.

There two sub-directories, each contain the files

HectorLinear.sds
HectorDistortion.sds

When running the HectorConfigUtil:

    The former replaces the file of the same name being used at the moment.
    The later replaces the file of the name tdFdistortion0.sds



fit2-b-pos              Fitting Rotation, Scale, centre of distortion offset NOT negated
fit2-b-neg              Fitting Rotation, Scale, centre of distortion offsets negated

